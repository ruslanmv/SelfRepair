## SelfRepair: {{ summary }}

**Finding:** `{{ finding.kind }}` ({{ finding.severity }})  
**Rule:** {{ finding.rule_id or "deterministic" }}  
**Risk:** {{ plan.risk }}  
**Files changed:** {{ files_changed | length }}

### Why
{{ rationale }}

### What changed
{% for f in files_changed -%}
- `{{ f }}`
{% endfor %}

### How to revert
```bash
git revert {{ commit_sha }}
```

### Provenance
- Builder: `{{ provenance.builder }}`
- Run: [{{ provenance.run_id }}]({{ provenance.run_url }})
- Materials hash: `{{ provenance.materials_hash }}`
- {% if provenance.signed %}Signed via Sigstore (verify: `cosign verify-blob`){% else %}Unsigned{% endif %}

---

🤖 Generated by [SelfRepair]({{ selfrepair_url }}). To opt out for this repo, add
`kind: {{ finding.kind }}` to `.selfrepair.yml` under `wont_fix:`.
